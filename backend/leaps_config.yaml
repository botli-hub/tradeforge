# ⚠️ 本文件已停用,仅作历史存档 —— 后端不再读取此文件。
#
# 所有配置(Telegram、Finnhub/Yahoo/Futu 数据源、扫描/信号参数、Wheel 参数等)
# 已迁移到「设置页」,保存在本地数据库(backend/app/tradeforge.db 的 app_kv 表)。
# 请打开前端「设置」页面填写并保存,后端会立即读取数据库中的最新值。
#
# 安全提醒:本文件此前曾明文写入过一个 Telegram Bot Token 并被提交到代码仓库,
# 已在此次清理中删除。该 token 已经暴露在 git 历史中,请尽快到 @BotFather
# 执行 /revoke 或 /token 重置,并在设置页填入新的 token。
#
# 股票池/LEAPS 监控白名单也已改为完全由数据库管理,请在「股票池」「LEAPS」
# 页面中手动添加标的,不再从本文件读取。
